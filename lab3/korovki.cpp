#include <bits/stdc++.h>
using namespace std;

vector<int> coords;
bool check(int x){
    int cows = 1;
    int last_cow = coords[0];

    for(int i = 1; i < coords.size(); i++){

    }
}

int main() {
    int n, a, k;
    cin >> n >> k;
    while(n--){
        cin>>a;
        coords.push_back(a);
    }

    int l = 0;
    int r = coords[coords.size()-1] - coords[0] + 1;

    while(l <=  r) {

    }


    return 0;
}